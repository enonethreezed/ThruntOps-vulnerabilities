# ad_rdp_domain_controller

Adds existing domain principals to the declared Remote Desktop Users group on a
domain controller. Run on a DC with the ActiveDirectory module.

```yaml
ad_rdp_domain_controller_principals: [lab_user]
```
