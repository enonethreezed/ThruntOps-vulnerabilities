# windows_badsuccessor_vulnerable

Validates, without changing the host, an explicitly declared eligible Server
2025 build range and rejects any declared mitigating KB. Defaults intentionally
fail closed until the exact lab compatibility policy is supplied.
